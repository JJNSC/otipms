<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.otipms.dao.ProjectDao">
	
	<insert id="addProject" parameterType="Project">	
		<selectKey keyProperty="projectNo" resultType="int" order="BEFORE">
			select project_seq.nextval from dual <!-- 시퀀스의 다음 번호를 가지고온다. 그리고 다음단계=>세팅하기 -->
		</selectKey>
		INSERT INTO project (projectNo, projectName, projectStartDate, projectEndDate, projectStatus, projectCompanyName, projectContent, projectDeleted, empId)
		 values (#{projectNo},#{projectName},#{projectStartDate},#{projectEndDate},#{projectStatus},#{projectCompanyName}, #{projectContent},#{projectDeleted},#{empId})
	</insert>
	
	<!-- 모든 프로젝트 select -->
	<select id="selectAllProject" resultType="Project">
		select * from project where projectNo >0 order by projectNo desc
	</select>
	
	<select id="getEmployeeInfoByProjectNoAndRoleNo" parameterType="Employee" resultType="Employee">
		select e.empId, e.empName, e.empRank, e.role, eif.teamno,eif.empTel, eif.empEmail from emp e 
		inner Join
		(select empId, teamNo, empTel,empEmail from empInfo where teamNo in 
					(select teamNo from team where projectNo =#{projectNo})) eif 
		            on e.empId = eif.empId where e.role=#{role}
	</select>
	
	<select id="selectProjectByProjectNo" parameterType="int" resultType="Project">
		select * from project where projectNo = #{projectNo}
	</select>
	
	
</mapper>